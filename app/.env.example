# Local Flask stand-ins (default `npm run dev`).
# WAITING BFF: these are not platform routes. The UI keeps calling /api/app,
# /api/parts, /api/plyworks; Vite proxies those prefixes to the hosts below.

VITE_BOXOUT_BACKEND_URL=http://127.0.0.1:5000
VITE_SIMPLEPARTS_BACKEND_URL=http://127.0.0.1:5001
VITE_PLYWORKS_BACKEND_URL=http://127.0.0.1:5002
